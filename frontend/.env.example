# URL do backend FastAPI. Deve ser publica (NEXT_PUBLIC_) porque o fetch
# acontece no browser. Nao coloque segredos aqui — as chaves de IA ficam
# exclusivamente no backend.
NEXT_PUBLIC_API_URL=http://localhost:8000
